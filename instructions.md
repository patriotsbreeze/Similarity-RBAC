make sure you do all of these things. 

Project Execution Plan1.Environment Setup & Baseline Integration:Literature Foundation: ACORN & FilteredVamana.Claude Code begins by setting up a C++ development environment and forking hnswlib, the industry-standard header-only C++ library for HNSW.Agent Tasks:Write build scripts (CMake) and establish the testing framework.Implement the two standard literature baselines to compare against:Post-filtering: Standard HNSW search where the agent retrieves a large candidate set and discards vectors the user lacks permission to see.Pre-filtering: A brute-force linear scan computing distances only on vectors the user is authorized to access.2.Synthesize Biomedical Benchmark Data:Workload Modeling.Because Claude Code cannot access real protected health information (PHI), it must mathematically model the distribution of clinical data and hospital access patterns.Agent Tasks:Write a Python/NumPy script to generate 1 million 768-dimensional float arrays, simulating BioBERT clinical embeddings.Generate synthetic RBAC tags using a 64-bit integer bitmask for each vector. The bitmask will simulate a complex hospital hierarchy (e.g., Oncology Dept + Researcher Role + Patient Consent Flag).Ensure the data distribution creates varying levels of selectivity (e.g., some roles can access 50% of the database, while strict privacy roles can only access 0.1%).3.Implement RBAC-HNSW Core Logic:Algorithmic Innovation.This is the core contribution of the paper. Instead of building multiple separate indexes (as seen in SIEVE, VLDB 2025), Claude Code will modify the graph traversal algorithm to evaluate access rights at the node level dynamically.Agent Tasks:Modify the internal HNSW Node struct to attach the uint64_t rbac_mask adjacent to the vector data in memory to prevent cache misses.Rewrite the greedy search heuristic (the searchKnn function). Before executing the expensive Euclidean/Cosine distance mathematical operations, insert a bitwise AND check: (node_mask & user_query_mask) == user_query_mask.If the node fails the access check, the algorithm skips the distance calculation but still uses the node's edges to route through the graph, preventing the "dead end" problem described in filtered vector search literature.4.Execute Crucial VLDB Experiments:Empirical Evaluation.A VLDB systems paper requires rigorous benchmarking across multiple axes. Claude Code will write a test harness to run the following specific experiments using the synthetic dataset.Agent Tasks (The Experiments):Selectivity vs. Recall: Measure how accurately the RBAC-HNSW finds the true nearest neighbors as the query becomes more restrictive (testing 1%, 5%, 20%, and 80% database accessibility).Selectivity vs. QPS (Queries Per Second): Prove that RBAC-HNSW maintains high QPS across all access levels, whereas the baselines will crash (Post-filtering fails at low selectivity; Pre-filtering fails at high selectivity).Memory Overhead: Measure the RAM footprint. Add the bitmask overhead to the baseline HNSW memory and compare it against the multi-index architectures proposed by competitors like SIEVE.5.Iterative Profiling and Systems Optimization:Hardware-Level Tuning.To elevate the paper from a "good idea" to a "VLDB-accepted systems paper," the implementation must be highly optimized at the CPU level.Agent Tasks:Run Linux perf stat on the benchmarking suite to identify CPU bottlenecks.Cache Line Optimization: Claude Code will refactor the memory layout to ensure the rbac_mask and the vector data fit perfectly within a 64-byte CPU cache line, minimizing L1 cache misses.SIMD Vectorization: If the access control logic becomes complex (e.g., moving from a single 64-bit mask to an array of masks), the agent will rewrite the bitwise checking loops using AVX-512 intrinsic instructions for parallel processing.